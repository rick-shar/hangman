import PropTypes from 'prop-types';
import React from 'react';

const Game = ({ data }) =>
    <div>
        <h1> Game </h1>
    </div>;

ProductRow.propTypes = {
    data: PropTypes.object
};

export default Game;
